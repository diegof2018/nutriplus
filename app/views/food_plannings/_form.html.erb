<%= form_with(model: food_planning, local: true) do |form| %>
  <% if food_planning.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(food_planning.errors.count, "error") %> prohibited this food_planning from being saved:</h2>

      <ul>
      <% food_planning.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :patient_id %>
    <%= form.text_field :patient_id %>
  </div>

  <div class="field">
    <%= form.label :date_start %>
    <%= form.datetime_select :date_start %>
  </div>

  <div class="field">
    <%= form.label :date_finish %>
    <%= form.datetime_select :date_finish %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
