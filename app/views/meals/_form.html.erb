<%= form_with(model: meal, local: true) do |form| %>
  <% if meal.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(meal.errors.count, "error") %> prohibited this meal from being saved:</h2>

      <ul>
      <% meal.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :type_meal_id %>
    <%= form.text_field :type_meal_id %>
  </div>

  <div class="field">
    <%= form.label :values %>
    <%= form.text_field :values %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
